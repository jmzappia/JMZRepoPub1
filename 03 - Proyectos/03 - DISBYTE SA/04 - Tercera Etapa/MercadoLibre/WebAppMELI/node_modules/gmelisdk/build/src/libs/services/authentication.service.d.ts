export declare const modelAuthMeli: (code: string | undefined) => {
    grant_type: string;
    client_id: string;
    client_secret: string;
    code: string;
    redirect_uri: string;
};
export declare const modelAuthRefreshToken: (refresh_token: string) => {
    grant_type: string;
    client_id: string;
    client_secret: string;
    redirect_uri: string;
    refresh_token: string;
};
export declare const authenticateByCode: (code: string) => Promise<any>;
export declare const authenticateByRefreshToken: (refresh_token: string | undefined) => Promise<any>;
