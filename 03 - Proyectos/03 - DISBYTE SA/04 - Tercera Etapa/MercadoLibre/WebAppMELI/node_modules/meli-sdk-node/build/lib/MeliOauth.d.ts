import { MeliApiError, MeliOauthResponse } from "./types";
interface GetAuthUrlOptions {
    state?: string;
    auth_url: string;
    redirect_uri: string;
    client_id: string | number;
}
interface MeliAppConfig {
    redirect_uri: string;
    client_id: string | number;
    client_secret: string;
}
export declare class MeliOauth {
    private static http;
    static getAuthUrl({ auth_url, state, redirect_uri, client_id, }: GetAuthUrlOptions): string;
    static authorize(code: string, appInfo: MeliAppConfig): Promise<MeliApiError | MeliOauthResponse>;
    static refreshAccessToken(refresh_token: string, appInfo: Omit<MeliAppConfig, "redirect_uri">): Promise<MeliApiError | MeliOauthResponse>;
}
export {};
